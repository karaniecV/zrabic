import { Focus2Directive } from './focus2.directive';

describe('Focus2Directive', () => {
  it('should create an instance', () => {
    const directive = new Focus2Directive();
    expect(directive).toBeTruthy();
  });
});
