import { Focus1Directive } from './focus1.directive';

describe('Focus1Directive', () => {
  it('should create an instance', () => {
    const directive = new Focus1Directive();
    expect(directive).toBeTruthy();
  });
});
